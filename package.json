{
  "private": true,
  "devDependencies": {
    "http-server": "0.10.0",
    "lerna": "^2.0.0-rc.5",
    "prettier": "^1.4.4"
  },
  "scripts": {
    "start": "http-server -c-1 -s",
    "format": "prettier --write **/*.js"
  }
}
